import { DashLayout } from "./DashLayout";

export default {
  title: "Components/DashLayout",
  component: DashLayout,
};

const Template = (args) => <DashLayout {...args} />;

export const Default = Template.bind({});
Default.args = {};
